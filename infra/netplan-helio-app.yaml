# A copier sur helio-app dans /etc/netplan/99-helio.yaml puis :
#   sudo chmod 600 /etc/netplan/99-helio.yaml && sudo netplan apply
network:
  version: 2
  ethernets:
    enp0s3:
      dhcp4: true
    enp0s8:
      dhcp4: false
      addresses: [192.168.56.11/24]

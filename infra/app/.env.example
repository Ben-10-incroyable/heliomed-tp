# Copier en .env sur helio-app : cp .env.example .env
# Le fichier .env n'est JAMAIS commite (voir .gitignore).
POSTGRES_DB=heliomed
POSTGRES_USER=helio_app
POSTGRES_PASSWORD=changez-moi-avant-le-premier-demarrage
